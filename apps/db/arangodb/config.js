const config = {
    database: 'fly-time',   // default database, if not set in env
    user: 'root',           // default user, if not set in env
    password: 'root'        // default password, if not set in env
}

exports.db = config